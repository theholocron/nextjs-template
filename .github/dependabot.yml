# AUTO-GENERATED — do not edit directly.
# Source:  theholocron/holocron · packages/cli/src/commands/setup.ts
# Tool:    holocron setup
# Changes: run `holocron setup` to regenerate.
version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
    groups:
      security-patches:
        applies-to: security-updates
        patterns:
          - "*"
      all-dependencies:
        update-types:
          - minor
          - patch
    ignore:
      # @commitlint/config-conventional requires() this package via CJS;
      # v10+ is ESM-only and crashes commitlint. Keep pinned at 9.x until
      # @commitlint/config-conventional adds ESM support.
      - dependency-name: conventional-changelog-conventionalcommits
        versions: [">=10"]

  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      all-actions:
        patterns:
          - "*"
